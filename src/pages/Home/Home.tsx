import React from 'react';
import './styles.css';

type HomeProps = {
  // Define props here
};

const Home: React.FC<HomeProps> = (props) => {
  return (
    <div>
      <h1>Home Component</h1>
    </div>
  );
};

export default Home;
